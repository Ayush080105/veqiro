export const GOOGLE_FEATURES_LOCKED =
  process.env.NEXT_PUBLIC_GOOGLE_FEATURES_LOCKED === 'true'

// Hardcoded (not env-driven) — flip this boolean and redeploy to lock/unlock.
export const VIDEO_FEATURES_LOCKED = true
